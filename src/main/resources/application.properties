spring.application.name=heail-backend

# ── Database
spring.datasource.url=jdbc:postgresql://ep-lingering-dew-aiidopzs-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_tQYuAcaO4M3v
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# ── JWT
jwt.secret=${JWT_SECRET:heail-secret-key-change-in-production-min-256-bits!!}
jwt.access-token-expiry-ms=86400000
jwt.refresh-token-expiry-ms=2592000000

# ── Mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:contactheail@gmail.com}
spring.mail.password=${MAIL_PASSWORD:changeme}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ── Server
server.port=8080
